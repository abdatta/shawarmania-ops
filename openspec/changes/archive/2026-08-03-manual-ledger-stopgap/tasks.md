## 1. Database contract

- [x] 1.1 Add one forward-only migration creating `manual_ledger_days` (unique on `(outlet_id, business_date)`, integer-paise revenue/cash/opening/counted columns, integer basis-point commission columns, cash-movement reason columns, optional note) and `manual_ledger_expenses` (reusing `public.expense_category`, plus `is_cash`, integer-paise amount, required free-text `description`), both with explicit `business_date date` columns. No capital marker on either table.
- [x] 1.2 Add check constraints refusing negative opening cash, counted cash and cash movements, a zero-or-negative expense amount, a blank-or-whitespace reason on a non-zero cash movement, a missing or blank-or-whitespace expense description, a blank-or-whitespace day note where one is given, a future business date, and a commission rate outside 0 to 10000 basis points. Permit negative revenue so a cash refund is recordable.
- [x] 1.3 Enable RLS on both tables with select/insert/update/delete policies predicated only on `public.app_is_owner() and public.app_account_active()`, with no outlet-role predicate.
- [x] 1.4 Add database and REST tests proving an owner reads and writes both tables at both outlets; a Franchise Admin, Biller and Employee are each refused select and insert at an outlet where they hold a live assignment; ending an owner assignment or deactivating the account ends access on the next request; a duplicate `(outlet_id, business_date)` is refused; and every check constraint refuses a hand-crafted violation.
- [x] 1.5 Classify both tables in the catalog-enumerating isolation suite as owner-only so the suite passes without weakening its unclassified-table failure.
- [x] 1.6 Reset the local schema, regenerate database types, and prove the generated snapshot is current.

## 2. Derivation module

- [x] 2.1 Implement one integer-paise derivation module computing expected cash (`opening + cash revenue + cash in − cash expenses − cash out`), the signed difference against the count, and per-day net aggregator revenue using `(stated * bp + 5000) / 10000` integer round-half-up.
- [x] 2.2 Implement monthly aggregation: gross revenue per channel, per-day-netted aggregator revenue summed across the month, expenses totalled by category, and a cash-basis operating profit estimate subtracting every recorded expense and never including commission as an expense.
- [x] 2.3 Implement the read-only opening-cash chain check comparing a day's stored opening against the preceding day row's counted cash, returning a signal and never a repaired figure.
- [x] 2.4 Unit-test the module on a month of mixed commission rates, a shortfall day, a surplus day, a balanced day, a refund recorded as negative cash revenue, a non-cash expense, a large cash-out for equipment leaving the drawer reconciled and the month's expenses untouched, a broken chain, and an empty month distinguished from a zero month. Assert the month's profit reconciles exactly against its expenses by category, so no expense is silently excluded.

## 3. Adapter and demo behavior

- [x] 3.1 Define the typed manual-ledger adapter interface (read a day, upsert a day, list and mutate expenses for a day, read a month) in `src/data-access/adapters.ts`, as its own deletable section. **Corrects design D5**, which placed the interface and both implementations in `src/features/manual-ledger/`: `docs/ARCHITECTURE.md` puts every adapter interface in `adapters.ts`, mocks in `mock/` and real adapters in `supabase-adapters/`, and `DataAdapters` has to name this adapter for `useAdapters()` and both factories to reach it. Honouring D5 literally would have made `src/data-access/` import upward from `src/features/`, leaving a layering inversion behind after the stopgap was deleted. Retirement is still a deletion rather than an untangling: one section, two files, one `DataAdapters` line, one line per factory, all greppable by `manualLedger` / `manual_ledger`.
- [x] 3.2 Implement the Supabase adapter, using `on conflict (outlet_id, business_date) do update` for the day upsert.
- [x] 3.3 Implement the mock adapter and fixtures typed from generated schema types, thin but sufficient for the surface to render coherently and for the derivation paths to be exercised in demo mode.
- [x] 3.4 Add adapter and mock tests covering the day upsert, expense mutation, month read, and the defaults offered for a new day.

## 4. User interface

- [x] 4.1 Add one registry entry in the `live` state, one route, and one navigation item rendered only for a live Super Admin assignment.
- [x] 4.2 Build the day form: four revenue channels, cash in and out with required reasons, counted cash, note, and editable commission and opening-cash fields pre-filled from the most recent earlier day at that outlet, with both required and undefaulted on an outlet's first day.
- [x] 4.3 Build the expense entry and list for the selected day: category, cash or non-cash, amount, and the required description, with edit and delete. Show the description in the day's expense list and in the month's expenses by category, so a purchase is identifiable without opening it.
- [x] 4.4 Build the day reading: expected cash, counted cash, the difference with its direction stated in distinct words for a shortfall and a surplus, the note beside it, and the chain signal where the opening disagrees with the previous count.
- [x] 4.5 Build the month reading: gross revenue by channel, aggregator revenue net of per-day commission, expenses by category, and the cash-basis profit estimate with its basis stated in words beside the figure as `profit-estimates` requires.
- [x] 4.6 Distinguish an unrecorded month from a recorded zero, and give every asynchronous region its own shimmer shape per the default loading contract.
- [x] 4.7 Add component and Playwright coverage for owner entry at both outlets, absence of the surface and its navigation for the other three roles, direct-path navigation rendering nothing, a retrospective commission edit changing profit but not expected cash, and an old-day count edit leaving later days' stored figures unchanged.
- [x] 4.8 Inspect the surface on phone and tablet viewports in light and dark themes with reduced motion, and confirm no new colour pair enters the contrast validator. Inspected at 375×812 and 1080×810 in both themes: no horizontal overflow, controls at the 42 px phone floor, and the contrast validator still reports the same 50 pairs, because every colour used is an existing token pair. Reduced motion needed no new guard — the surface composes only `Shimmer` and `LoadingRegion`, whose `motion-reduce:animate-none` is what carries it.

- [x] 4.9 Collapse a recorded day's entry card to a read-only reading in the same idiom as the drawer card, with Edit and Cancel, and move each cash movement's reason onto the drawer reading beside the amount it explains (design D11). Owner-requested on 2026-08-04 after seeing the drawer card. The read card carries the revenue side only, because every cash figure is already on the card below it.

- [x] 4.10 Lay the entry form out for a phone (design D12): money fields two to a row with the unit inside the box, each aggregator as one outlined block showing its stated figure, its rate and the net computed as they are typed, explanations behind a control beside each section title, and short visible labels whose accessible names still name the aggregator and the unit. Owner-requested on 2026-08-04. Caught while measuring: a `text-sm` on the inputs overrode the base layer's 16px rule and would have made iOS Safari zoom the viewport on focus — the height is now the phone control token and the font size is left alone.

- [x] 4.11 Bring the controls above the form onto the attendance idioms (design D13): a period stepper in the range-picker's shape, the day as an untypable button that opens the platform calendar and reads `Today` or `03 Aug 2026`, a two-segment view toggle, and the shared outlet switcher as a chip per outlet in both of its modes. Rename the day and month revenue sections to `Sales breakdown` and move Ledger ahead of People in the owner's navigation. Owner-requested on 2026-08-04. The switcher change touched every outlet-scoped suite, so the assertions now go through `src/test/outlet-scope.ts`.

- [x] 4.12 Shorten the cash badge on an expense row to the single word *Cash*, keeping *— from the drawer* in the badge for a screen reader. Owner-requested on 2026-08-04. Applied to the live expenses surface as well as this one, because the same badge marks the same fact and leaving them different is the inconsistency worth avoiding. Three assertions moved with it: `getByText` matches an element's own text nodes, so a phrase split across a visible word and an `sr-only` span no longer matched — each now asserts the visible half and the announced half separately, which is the better assertion.

- [x] 4.13 Give cash one set of words on this surface: *Cash from sales*, *Cash added*, *Cash expenses*, *Cash withdrawn*, with the form's fields, the reason prompts, the hint and the month's prose all using them. Owner-requested on 2026-08-04. The drawer reading had said *Cash taken* for sales and *Cash taken out* for a withdrawal — three of the same words for two opposite movements, on the one card that has to be read at speed at close.

## 5. Durable contract and verification

- [x] 5.1 Merge the approved requirements into `openspec/specs/manual-ledger/spec.md` and update `docs/DATA_MODEL.md`, `docs/ROLES_AND_PERMISSIONS.md`, `docs/SCREENS.md`, `docs/OPERATIONS.md`, `docs/LIMITATIONS.md` and `docs/TESTING.md`. `OPERATIONS.md` gained a step 9 to outlet onboarding and a nightly *Recording a trading day by hand* runbook; `SCREENS.md` records that Ledger takes the sixth owner tab and why.
- [x] 5.2 Record the retirement obligation in `openspec/changes/daily-cash-live/proposal.md` so #12 carries these rows into the live cash and expense records before the tables are dropped, and state the same exit in `docs/LIMITATIONS.md`. Written into #12 as an *Inherited obligation* section naming both halves: carry the rows, and do **not** inherit the owner's cash-write permission. It also names the two shapes that need a decision rather than a copy (a stored opening cash where a real drawer derives it, and aggregator revenue with no bill behind it).
- [x] 5.3 Add the roadmap row for this change and run `npm run roadmap:sync`. Row #36 reconciled `proposed → active`.
- [x] 5.4 Run formatting, lint, typecheck, unit tests, contrast validator, and the production build. Prettier clean; eslint 0 errors (2 pre-existing fast-refresh warnings, neither in this change); typecheck clean; **912 unit tests pass** after the interface work in 4.9–4.13 (903 at first implementation, plus nine covering the recorded-day reading, the hint disclosure, the aggregator blocks and the period controls); contrast still 50 pairs, no new pair; build clean.
- [x] 5.5 Run the Docker-backed database reset, the DB/RLS/REST suites including the new owner-only cases, the auth E2E suite, and the generated-type drift check. **775 pgTAP** (21 files), **160 REST/RLS** (152 before, plus 8 new manual-ledger probes over real tokens), **198 Playwright**, **16 auth E2E**; regenerating the types twice produced an identical file, so the snapshot is current. `12_required_fields_not_blank.sql` failed first and correctly: it keeps an explicit inventory of every `_not_blank` constraint and named the two new ones as unaccounted for. Both are now listed with why one is required and one optional, and the two cash-movement reason constraints are asserted separately because their names deliberately do not match the convention.
- [x] 5.6 Confirm no live surface queries a `manual_ledger_` table, and that the live owner cash boundary (no remote cash expense, withdrawal or day close) still refuses at the database. Every `manualLedger` reference under `src/features/` is inside `src/features/manual-ledger/`; no live surface, and nothing outside the folder imports the derivation module. The cash boundary is unchanged and still proved by `17_owner_reach.sql`, which passes.
- [x] 5.7 PHASE GATE — prove the ROADMAP checkpoint: the owner records a full trading day at each outlet from a phone and reads that day's difference and the month's cash-basis profit with its basis named; the other three roles are refused both tables by a hand-crafted request; an earlier day's edit moves no later day's stored opening cash, commission rate or expected cash; and the four-role demo walkthrough still walks.

  All five clauses proven:

  - **A full trading day at each outlet, from a phone.** `e2e/manual-ledger.spec.ts` records four channels, a cash movement with its reason, an expense with its description and a count at 390×844, reads the difference as *balanced* and then as *short*, and saves. A separate spec proves Kanchrapara inherits nothing and keeps its own row. Inspected by hand in the browser as well: the ₹7,950 opening and the 18% Zomato rate arrived inherited from the previous day.
  - **The month's basis, named on screen.** *Cash basis operating estimate*, asserted in both the component and Playwright suites, with the arithmetic checked by hand — August's Zomato commission reads ₹1,468.35 from two different daily rates where one rate on the month's total would have given ₹1,649.25.
  - **A large purchase from the drawer.** `ledger.test.ts` records a ₹40,000 fridge as cash taken out with its reason: the day reconciles, the month's expenses stay empty, and the profit is untouched.
  - **The other three roles refused.** `21_manual_ledger.sql` refuses each of them select and insert on both tables **at the outlet they hold**, and the new REST probes repeat it over HTTP with real tokens. `01_schema_coverage.sql` adds the catalog facts: eight policies, every one naming `app_is_owner()`, none naming an outlet-role predicate.
  - **An earlier day's edit moves no later day.** A component test corrects the earliest recorded day's count and asserts every later day's stored row is `toEqual` what it was; another changes a past day's commission and asserts the month's profit moves while expected cash does not.
  - **The four-role walkthrough still walks.** The whole Playwright suite passes, `e2e/demo.spec.ts` included, with its no-request-leaves-the-origin tripwire intact.
