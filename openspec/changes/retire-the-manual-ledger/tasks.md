## 1. Before anything is touched

- [x] 1.1 Dump `manual_ledger_days` and `manual_ledger_expenses` from production to a file outside the repo, following the snapshot procedure already used for the pre-staff-as-accounts baseline; verify the dump restores into a scratch database and row counts match production exactly.
- [x] 1.2 Establish the figures the carry-over must reproduce. The row-level baseline was read on 2026-08-26 and is tabulated in `design.md`: 19 day rows and 42 expense rows at Kalyani, 19 and 65 at Kanchrapara, with 1 and 9 voided and 27 and 47 recorded from away. **Re-read it immediately before drafting the migration**, because the notebook is still being written to, then add the monetary totals it does not yet carry: each outlet's monthly cash and non-cash expense sums and each day row's counted cash. Verify both the fresh counts and the totals are written into the change notes.
- [x] 1.3 Confirm #11 has been in production use long enough for the owner to say so, and that the derived statement and the notebook have been compared on real trading days; verify the owner's go-ahead is recorded with its date.
- [x] 1.4 Re-count how many `manual_ledger_days` rows carry a non-zero cash added before drafting the migration, and confirm each has a usable `cash_added_reason`; verify they carry as negative collections under task 3.3. It was exactly two on 2026-08-26, one per outlet, and the count no longer changes the modelling: signed amounts handle any number of them.
- [x] 1.5 Settle open question 1 with the owner: whether a consumption basis is still wanted now that inventory is shelved; verify the answer is reflected in the `profit-estimates` delta before it is applied.
- [x] 1.6 Settle open question 3: whether the archived day table stays in `public` with no grant or moves to a schema nothing reads; verify the decision is recorded.

## 2. Promote the expense record

- [x] 2.1 Remove the single remaining reference to `public.expenses` in `src/data-access/supabase-adapters/expense-categories.ts` and confirm no other reader exists; verify a repository-wide search finds none and the type check passes with the reference gone.
- [x] 2.2 Drop the empty `public.expenses`, having asserted in the same transaction that it holds zero rows; verify the assertion aborts the migration if any row exists.
- [x] 2.3 Rename `manual_ledger_expenses` to `expenses`, carrying its policies, indexes, triggers, constraints and comments; verify no row is inserted or deleted, every identity is preserved, and the void, attribution and recorded-from-away columns arrive intact.
- [x] 2.4 Re-point every adapter, query, mock and fixture at the promoted name; verify `npm run typecheck` passes and no code path names the old table.
- [x] 2.5 Confirm the expenses surface and the shared expense component behave identically after the promotion, in **both** the shapes `expenses-open-to-any-day` gave them: verify a staff member still corrects only their own rows on the day they recorded them and a correction outside that window is still refused, **and** that the owner and a manager still step or pick to any past business date, still correct and withdraw a row recorded by somebody else, and still record against the day on screen rather than today. The second half is the half the promotion is most likely to break, because it reaches rows through the renamed table on dates no other surface asks for.
- [x] 2.6 Regenerate `src/data-access/database.types.ts`; verify `npm run db:types` leaves no unexpected generated-type diff.

## 3. Carry the day rows across

- [x] 3.1 Carry each `manual_ledger_days` row into a drawer observation marked legacy imprecise, placed at that outlet's cutover boundary for its business date; verify the marker is distinct from the ordinary approximate marker and that no fabricated time of day is stored or shown.
- [x] 3.1a Apply the counted-figure conversion from design decision 0: `observation.counted_total = counted_cash_paise + cash_removed_paise`, with the removal recorded separately and the next opening equal to `counted_cash_paise`. **The notebook counts the drawer after the collection and this model counts it before**; a direct copy understates every carried observation by that day's collection. Verify the identity holds per row inside the migration, and verify against a hand-worked Kalyani day that the carried observation, its cash out and the following opening reproduce the notebook's own expected-cash arithmetic in `src/features/manual-ledger/ledger.ts`.
- [x] 3.1b Carry the 15 counted rows per outlet as observations and leave the 4 uncounted rows per outlet without one; verify a carried date that never had a count renders as `carried` rather than as an observation of zero.
- [x] 3.2 Carry cash removed into drawer cash out, as a spend where it had a reason and a collection where it did not; verify the reason survives and the instant matches the observation's.
- [x] 3.3 Carry each row's cash added as a **negative** cash out of kind `collection` at the observation's instant, retaining `cash_added_reason`; verify the drawer balance rises by that amount, the observation's arithmetic closes, and the reason is readable on the row. No bespoke shape is needed: #11's signed amounts already express inbound cash.
- [x] 3.4 Chain the carried observations so each opening is the previous one's carry-forward, and reconcile the chain against the notebook's stored openings; verify any disagreement is reported in the migration output rather than silently repaired. **Expect disagreements**: the production chain already breaks at Kalyani on 2026-08-12 and 2026-08-14, and across the 2026-08-18 to 08-20 gap. A migration that reports zero breaks against this data has repaired something it should have reported.
- [x] 3.5 Assert the reconciliation from task 1.2 inside the migration transaction, before anything is archived or dropped; verify a deliberately corrupted fixture aborts the whole transaction and leaves every source row untouched.
- [x] 3.6 Assert attribution completeness inside the same transaction: every carried row has a recording account, every corrected row a correcting account, every voided row its reason; verify a row missing any of these raises rather than carrying across unattributed.

## 4. Archive, then remove

- [x] 4.1 Rename `manual_ledger_days` to its archive name, read-only, with no client grant, per the decision from task 1.6; verify no policy grants any role access and no application code queries it.
- [x] 4.2 Drop `daily_cash_records` and `cash_withdrawals`, having asserted zero rows in each within the same transaction; verify the assertion aborts if either holds a row, and that no drawer cash out was ever recorded there after #11 shipped.
- [x] 4.3 Drop `close_business_day()`, `billing_assert_day_ready()`, `counter_shift_closed_day_guard()` and its trigger; verify no remaining function, trigger or policy references any of them and that a counter shift can still be opened on any date.
- [x] 4.4 Drop `outlets.billing_live_from` with its guard function and triggers; verify no reader remains and that the Outlets form no longer offers a counter-billing start date.
- [x] 4.5 Remove the manual ledger route, its own surface (`manual-ledger-surface.tsx`, `ledger-day.tsx`, `ledger-month.tsx`, `ledger.ts`), adapters, mock fixtures and its entry in `src/gates/registry.ts`; verify the route no longer resolves in either mode and the four-role demo walkthrough still walks.
- [x] 4.5a **`expense-list.tsx` and `field.tsx` are re-homed, not removed.** `src/features/manual-ledger/` carries a comment in three places saying its whole folder goes when this change lands, and for the notebook's own files that is right. It is wrong for these two: `ExpenseList` is the live expense list and entry form for every role, imported by `src/features/expenses/outlet-expenses-surface.tsx` and by `src/features/counter/counter-shell.tsx` — the tablet — both of which outlive this change. Move them to `src/features/expenses/`, update the three importers, and correct the stale "whole folder goes" comments in `src/components/ui/period-bar.tsx` and `src/features/cash/ledger-statement-surface.tsx` so the next reader is not told the folder took the list with it. Verify the tablet still records an expense and the four-role walkthrough still walks.
- [x] 4.6 Confirm the migration's order matches design decision 6, with the archive before every drop; verify a review of the migration reads dump, promote, carry, assert, archive, drop, in that order.
- [x] 4.7 Write and test a down-migration that restores the notebook's names and the dropped objects from the archive and the dump; verify it runs green against a scratch database restored from task 1.1, and record that it is not expected to be used.

## 5. Reporting and the loose ends this change settles

- [x] 5.1 Apply the `profit-estimates` delta per task 1.5; verify the profit surface names one basis, presents a ceiling while any commission is undetermined, and offers no control implying a consumption basis exists.
- [x] 5.2 Confirm the derived ledger covers every date the business has traded, from one reader; verify a date preceding an outlet's first bill renders through the same code path, marked legacy imprecise, and that no archived table is queried.
- [x] 5.3 Reconcile August 2026 end to end after the migration: verify each outlet's month reads the same totals from the derived statement as the notebook held before it, and record the exact figures alongside the ones from task 1.2.
- [x] 5.4 Close `openspec/todos/ledger-handover-per-outlet.md`, stating that the act it tracked became unnecessary rather than being performed; verify the reason is written into the closure so a future reader is not left guessing.
- [x] 5.5 Close `openspec/todos/raw-materials-is-identified-by-a-word-nobody-types.md` as dissolved with the consumption basis; verify the closure names what dissolved it.
- [x] 5.6 Review the remaining expense todos against the promoted table: `expense-payment-method-inherits-the-bill-enum.md` and `near-miss-category-matching-reaches-expenses.md` both named a change that no longer exists; verify each is either re-pointed at a real trigger or closed with a reason.

## 6. Docs, roadmap and gates

- [x] 6.1 Update `docs/DATA_MODEL.md`, `docs/SCREENS.md`, `docs/LIMITATIONS.md`, `docs/ROLES_AND_PERMISSIONS.md`, `docs/OPERATIONS.md`, `docs/DEMO_MODE.md` and `docs/TESTING.md`; verify no page still describes the manual ledger as a current surface, that `docs/LIMITATIONS.md` records the stopgap as discharged with the date, and that `docs/OPERATIONS.md` no longer instructs anybody to set an opening float or a counter-billing start date.
- [x] 6.2 Amend the #12 row and the Wave E narrative in `openspec/changes/ROADMAP.md`, including the standing note explaining why #36 sits in Wave D; run `npm run roadmap:sync` and verify no hand-stamped status drift.
- [x] 6.3 Read `.github/workflows/` and run every job it runs; verify the non-Docker and Docker-backed gates each pass with recorded evidence.
- [x] 6.4 Walk both real shells and all four demo roles on a phone and a tablet in both themes; verify expenses, the drawer, the ledger, billing, attendance and the aggregator surfaces are all unaffected except where this change intended.
- [x] 6.5 PHASE GATE — Retire the manual ledger: August 2026 reads from the derived statement with the same monthly totals, row counts and counted-cash figures the notebook held, asserted inside the migration so a mismatch aborts it whole; a carried observation's counted total is its source row's count **plus** that day's collection, because the notebook counted the drawer after the collection and this model counts it before, proved against a hand-worked day rather than in aggregate; the chain breaks that already exist in the production data are reported rather than repaired; every carried row keeps its recording account, its correcting account, its void state and reason, and its recorded-from-away marker, proved row by row rather than in aggregate; a date before an outlet's first bill renders through the same reader as yesterday, marked as an hour that was never recorded rather than given a plausible time; the expense record is one table, promoted by rename with no row copied, and the empty one is gone; `daily_cash_records`, `close_business_day()`, `billing_assert_day_ready()`, the closed-day shift guard and `outlets.billing_live_from` are all dropped with no reader left behind; the notebook's route no longer resolves while its rows survive read-only under an archive name; a down-migration restores the previous estate from the dump and runs green against a scratch database; the handover and raw-materials todos are closed with their reasons stated; and the four-role demo walkthrough still walks.
