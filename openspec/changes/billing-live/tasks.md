## 1. The menu becomes real

- [x] 1.1 Promote `admin-menu` to `live` with full create, rename, reprice, reorder, availability and retirement, prices entered and stored in integer paise, scoped by assignment.
- [x] 1.2 Add RLS tests proving a manager writes only their own outlets' menu and reads no other outlet's.
- [x] 1.3 Prove from an empty database that an outlet's whole menu can be created through the UI with no SQL, and that a retired item leaves every captured line unchanged.
- [x] 1.4 Rebuild the manager menu screen around **items**, not categories, on the pattern the expense list already uses: no separate create-a-category step and no empty category to leave behind. One right-aligned `+ Add` at the top right meaning add an item; the category is a free-form field on the item form that suggests existing categories and creates an unrecognised one on the way through, confirming before it does so that "Burger" beside "Burgers" is deliberate. A new category takes `max(sort_order) + 1`, and the manager's menu screen gains the explicit reorder action this change's spec already promises — the counter's grouping order is a decision the manager makes, not an accident of typing order. Scroll a newly added item or category into view and highlight it briefly, respecting `prefers-reduced-motion` for the highlight but not the scroll; build it as a reusable primitive, because [`reveal-what-was-just-added`](../../todos/reveal-what-was-just-added.md) wants the same behaviour on every other add flow.
- [x] 1.5 Give every item row one actions menu on the right rather than loose Turn off and Edit buttons, with the price immediately left of it; put an `OFF` chip beside the item's name, and render an unavailable row in the disabled treatment the deleted-expense row uses, minus the strikethrough.
- [x] 1.6 Replace the unconditional new-category warning with a real near-match check, because the owner is about to type both outlets' whole menus in one sitting under 1.6's gate and a warning that fires on every category is a warning nobody reads by the fourth item. Add a pure domain matcher that folds case, accents, punctuation and spacing and then looks for the shapes a mistyped category actually takes — the same name spelled differently, a singular beside a plural, a transposition or a dropped letter, and one name sitting inside another. Show the candidates it finds **inside the confirmation itself as choices**, so a near miss is fixed where it is caught rather than by cancelling and retyping; choosing one files the item under the existing category with its existing spelling. Where nothing matches, create the category with no dialog at all. Fall the category field's own suggestion list back to the same matcher when its substring filter finds nothing.
- [ ] 1.7 GATE: both outlets' real menus are entered through the app by the owner before any tablet is set up.

## 2. Local delivery store

- [x] 2.1 Add the versioned Dexie envelope, dependency, result, tombstone and lease records, with migrations that preserve unsent data across application updates.
- [ ] 2.2 Preserve transactional local acceptance so a command is acknowledged and its form clears only after the IndexedDB commit, with a non-destructive blocking state when storage fails; remove the direct-payment delivery hold and queued-write Undo, make both payment paths eligible for delivery immediately, and chain each durably accepted payment correction behind its payment and prior correction without rewriting any envelope.
- [x] 2.3 Implement Web Locks leader election with the IndexedDB lease fallback, and dependency-aware draining that does not freeze unrelated order chains.
- [x] 2.4 Implement bounded retry and backoff driven by actual request evidence, with browser connectivity events used only as retry hints.
- [x] 2.5 Map accepted, exact replay, retryable, order-not-open, identity conflict, permanent refusal, corrected and discarded outcomes to durable local states; permit correction/discard only on the originating tablet under its live shift, retaining actor, time, reason and refused trace.
- [x] 2.6 Report the unsent count through `report_counter_device_state`, so the `last_reported_unsent` column and the Tablets surface column that #9 shipped start carrying real numbers, without logging payloads or customer phone numbers. **Moved here from #33 §4 on 2026-08-09**, with the rest of the store.

## 3. Real billing adapters

- [x] 3.1 Connect the live menu adapter so reachable sessions always fetch the latest outlet menu and a live shift falls back only after a real backend failure.
- [x] 3.1a Make "always fetch the latest" true of a screen that stays open, on two independent triggers. Re-read the menu and the activity rail when the billing screen returns to the foreground, reusing `useOnForeground` from `src/features/attention/attention.ts` rather than adding a second visibility listener; and subscribe to that outlet's `menu_categories` and `menu_items` through the shared-channel helper the counter adapter already publishes, treating what arrives as a nudge to re-read and never as the data. Neither trigger may be load-bearing alone: prove the foreground path still refreshes with the subscription torn down, which is the failure mode the counter adapter records losing a day to. Grant the channel what it needs on those tables and confirm it delivers under RLS, because a filter on an ungranted column goes quiet without erroring. A refresh must replace only the grid and the rail's lists: captured lines, the order under edit and any suspended draft survive it untouched.
- [x] 3.2 Connect customer exact lookup and create-or-get adapters without exposing directory browse, cross-outlet history, or phone values in logs.
- [x] 3.0 Narrow `public.payment_method` to `cash | upi`, following `20260811000002_remove_unsupported_payment_methods.sql` exactly: audit production first, refuse to run if any `swiggy` or `zomato` row exists in `bills` or `expenses`, move both columns through text, replace the type, restore the typed columns and the `expenses_insert` policy, then regenerate database types.

  Narrowing the type is what makes an aggregator bill impossible; narrowing the UI is what stops a biller looking for a button that is gone. Both are this task, and the type is the one that decides. Every place the four-value list is written out today:

  - `shared/billing-command.ts` — `BillingPaymentMethod`.
  - `src/features/billing/payment-dialog.tsx` — the `METHODS` touch targets, so Mark Paid opens two buttons and not four.
  - `src/features/billing/manager-billing-history.tsx:146` — the payment-method **filter** offers only what can be billed; there is no history of aggregator bills to filter for, because none was ever rung.
  - `src/data-access/mock/insights.ts:53` — `METHOD_ORDER`.
  - Shift and rail summaries, including the `shift-total-swiggy` and `shift-total-zomato` test ids, which go rather than sitting at ₹0.
  - `src/data-access/mock/fixtures/billing.ts` — five demo bills carry an aggregator method. Re-tender them as Cash or UPI rather than deleting them, so the demo keeps the same shape of day.
  - `e2e/counter.spec.ts` and `src/features/billing/billing-counter.test.tsx` — the Swiggy payment walkthrough, the four-method assertion and the aggregator shift totals are rewritten against the two supported methods, not deleted. A test that no longer names the withdrawn methods proves nothing about them; pair the rewrite with the refusal assertions in 8.1 and 8.5.

  **The manual ledger's own columns are untouched.** Its aggregator revenue and rates never used this enum, and they are now the only record those orders exist in.
- [ ] 3.3 Extend the local-first adapters for direct-payment and pay-order commands with one append-only `correct_bill_payment` envelope carrying the bill, expected effective revision and exact replacement Cash/UPI allocations; reject unsupported methods, keep the unchanged bill unnumbered until its original server result arrives, keep `discount_paise` zero, surface the correction locally before delivery, and retain the UI-only rule that either customer name or phone is entered before Order or Mark Paid.
- [ ] 3.4 Extend live shift-summary, revenue-business-date bill-history, manager void and manual counter re-ring reads so locally accepted payments appear in Bills this shift immediately, every surface and total uses the latest effective tender, eligible current-shift bills expose payment edit only on the originating tablet, and manager history remains read-only for payment while showing effective allocations; preserve every existing open-order and diagnostic boundary.
- [x] 3.5 Preserve typed adapter composition so screens import neither Supabase nor Dexie directly.
- [ ] 3.6 Add outlet-scoped append-only `bill_payment_corrections` and replacement-allocation records, RLS and isolation coverage, immutable guards, sequential per-bill revisions, the `correct_bill_payment` RPC and an effective-allocation read boundary. Lock and validate settled state, originating tablet and shift, expected revision, exact integer-paise arithmetic, supported methods and immutable command creation time inside the original stored `paid_at + 5 minutes`; make exact replay return the same revision and regenerate database types.

## 4. V1 session and cutover behaviour

- [x] 4.1 Keep unsent and needs-attention envelopes through the shift ending, cutover, browser restart and compatible app updates.
- [x] 4.2 Let an already-open counter continue from its shift's menu snapshot during a transient outage, with a persistent offline banner and captured line snapshots.
- [x] 4.3 Require the backend and a freshly approved shift to start or resume new billing after a reload, a missing shift or cutover, while still permitting old queue delivery and status.
- [x] 4.4 Stop draining once a tablet is removed, keep its envelopes, and warn on the Tablets surface before removing a tablet reporting unsent work.
- [x] 4.5 Add not-sent-yet, retrying, sent, needs-attention, void and cancelled indicators using semantic tokens in both themes.
- [ ] 4.6 Extend the online finish-day flow so it also refuses while any paid bill still carries its promised five-minute payment-edit window; then drain and verify the date, refuse while anything is unresolved, end the shift, write the end-of-day confirmation, and lock further work under it.

## 5. The ledger handover

- [x] 5.0 Add `outlets.billing_live_from date NULL` with its RLS and a Super Admin control that sets it, refusing a business date that has already started — a day that begins hand-typed and ends sourced from bills is the double-count 5.3 exists to catch. Regenerate database types.
- [x] 5.1 Source a live outlet's **cash and UPI** revenue from paid bills for business dates on and after that outlet's `billing_live_from`, each labelled on screen as coming from the counter, with those two typed fields removed for those dates.
- [x] 5.2 Leave earlier business dates, and an outlet that is not live yet, exactly as they are, each labelled for what it is.
- [x] 5.3 Add the test that fails if a live outlet's day or month counts cash or UPI revenue more than once.
- [x] 5.3a Add the test that fails if the Zomato or Swiggy revenue field, its commission rate or its computed net is removed, disabled or sourced from bills on a live outlet's day, and prove a live day saves with aggregator revenue and no typed cash or UPI figure. Withdrawing aggregator tender in 3.0 makes the ledger the only record of that trade, so this is the counterpart to 5.3: one test catches money counted twice, the other catches money recorded nowhere.
- [x] 5.4 Leave aggregator revenue and commission, cash in and out, expenses and the counted drawer on the manual path, and say in `docs/LIMITATIONS.md` that #12 and #13 own their retirement. State plainly there that **bills are not a complete record of revenue in V1**: aggregator trade is absent from them and lives only in the ledger, so any figure quoted from bills as total sales understates the outlet.

## 6. Gate promotion and demo preservation

- [x] 6.1 Promote tablet setup, counter billing, open orders, customer lookup and billing history from `demo` to `live` for their authorised contexts.
- [x] 6.2 Verify personal Biller sessions keep Employee navigation while the tablet exposes only billing pages under an approved shift.
- [ ] 6.3 Keep `/demo` on the synthetic #31 adapters, opening no real queue and making no Supabase write, while making immediate and on-handover payment edits, the relative five-minute countdown and expiry walkable through demo time controls.
- [ ] 6.4 Update `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/OFFLINE_AND_SYNC.md`, `docs/SCREENS.md`, `docs/DEMO_MODE.md`, `docs/OPERATIONS.md`, `docs/TESTING.md` and `docs/LIMITATIONS.md` for the exact V1 boundary, the append-only payment-correction model and the V2 follow-ups. Record the freshness rule where it can be found without reading the counter adapter: read on mount and on foreground, treat a reported change as a nudge to re-read and never as the data, and never let the subscription be the only path. `docs/ARCHITECTURE.md` states the badge convention of no subscription and no timer, and `docs/LIMITATIONS.md` explains the battery reasoning behind it, so both need the counter named as the deliberate exception and the reason it is one: a mains-powered tablet fixed on one screen where staleness is charged to a customer, not a lagging count.

## 7. Five-minute payment correction

- [ ] 7.1 Reuse the payment dialog in a correction mode prefilled from the bill's effective allocations, keeping sale facts locked and confirmation disabled until the replacement is changed and exact. On a new payment with no allocation, render Cash and UPI in the same neutral treatment while retaining Cash's non-colour banknote cue; do not rearrange the two buttons or fill their existing empty space.
- [ ] 7.2 Replace the transient direct-payment Undo with a brief success state and place both immediate and on-handover payments in Bills this shift at local acceptance. On each eligible expanded bill, render `Edit for N min` with minutes rounded up, switch to `Edit for N sec` below one minute, update at the displayed unit's boundary, and remove the action with no expired message at zero; editing never restarts the original deadline.
- [ ] 7.3 Apply each durably accepted local correction to that bill's effective tender and the tablet's Cash/UPI shift totals immediately, retain its not-sent-yet state, and reconcile to the authoritative accepted revision on delivery. Route server shift history, manager history, manual-ledger revenue, drawer cash and every other billing total through the shared effective-allocation boundary so the original and earlier revisions never count again.
- [ ] 7.4 Keep payment correction available only on the originating tablet under its live shift and only for tender. After expiry, or for wrong items, customer facts, totals, clocks or dates, show no edit action and retain the attributed manager void plus manual counter re-ring guidance with no personal-device payment command.

## 8. Verification and rollout

- [ ] 8.1 Update unit tests for local acceptance without a delivery hold; immediate and on-handover payment editing; unchanged bill identity; exact prefill and locked sale facts; Cash and UPI both neutral on an empty dialog; unchanged dialog geometry; `Edit for 5 min` at 4:01, `Edit for 1 min` at 1:00, `Edit for 59 sec` at 0:59 and disappearance at expiry; exact split-tender persistence; locally effective shift totals; dependency ordering; leader failover; retry classification; and local-store upgrades preserving unsent ancestors and corrections.
- [ ] 8.2 Update integration tests for upfront and on-handover payment corrections, repeated append-only revisions without deadline reset, correction before parent delivery, exact replay after response loss, stale-revision refusal, expiry to manager void/manual re-ring, finish-day refusal during an open edit window, effective revenue-date history, Cash-only drawer contribution and manager history using the latest tender. Preserve the existing menu/rail freshness, needs-attention and suspended-draft coverage.
- [ ] 8.3 Run browser tests that drop the backend before and after payment and correction local commits, lose each response after the server commits, restart with the payment/correction chain unsent, refuse to start new work offline, prove eventual exactly-once settlement and correction, and refuse finish-day until both the queue and the last five-minute window are resolved.
- [ ] 8.4 Run `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm test`, `npm run contrast`, `npm run build` and `npm run test:e2e`, then inspect the payment dialog and paid-bill card on phone and tablet layouts in light and dark. Confirm Cash has no false initial selection and that the intentionally unchanged two-method geometry has no regression.
- [ ] 8.5 Run `npm run db:start && npm run db:reset`, then `npm run test:db`, `npm run test:rls` and `npm run test:e2e:auth`. Prove original bills and allocations reject update/delete; correction rows are append-only and outlet-isolated; another tablet and hand-crafted late, backdated-outside-shift, stale-revision, unsupported-method and bad-arithmetic commands are refused; exact replay appends once; effective Cash/UPI totals use only the latest revision; the existing aggregator-enum narrowing proofs remain green; regenerate database types and require a clean generated diff.
- [ ] 8.6 Adversarial review pass: a separate session reads these spec deltas against the delivered schema, adapters, UI, gates, effective-allocation consumers and ledger handover, and reports every requirement it cannot find satisfied.
- [ ] 8.7 ROLLOUT — **Kalyani first**, which is the outlet that has a tablet: enrol it, enter its real menu through the app, run shadow smoke tests before any customer money, exercise an immediate and on-handover payment correction, set `billing_live_from` to a business date that has not started, promote, hand its cash and UPI revenue over, keep typing its aggregator revenue, and trade one full day closed cleanly. Only then repeat for Kanchrapara, whose hardware arrives later.
- [ ] 8.8 PHASE GATE — Billing V1 live: at both outlets, one tablet takes real immediate and on-handover Cash/UPI payments against a menu entered through the app; either payment appears in Bills this shift at local acceptance and permits only append-only tender correction for five minutes with the agreed relative label, same bill identity and database-enforced expiry; Cash is not falsely selected when tender capture opens and the dialog geometry is unchanged; accepted payments and corrections survive restart and land exactly once after forced response loss; manager void and manual re-ring remain the post-window or non-tender correction with no personal-device billing path; needs-attention work stays originating-tablet-only while manager diagnostics stay read-only; finish-day waits for both the edit window and queue; menu and cutover rules hold; the database refuses aggregator allocation; effective cash and UPI revenue each appear exactly once while Zomato and Swiggy remain typed with their rates; the real gates are live and the demo walkthrough is still isolated.
