## 1. Rehearsal and baseline evidence, before any migration

- [x] 1.1 Read `openspec/specs/daily-cash-reconciliation/spec.md`, `openspec/specs/manual-ledger/spec.md` and this change's `design.md` end to end before writing anything; verify the four decisions a fresh session most often gets backwards are understood: the carry-forward anchors to the counted figure (D3), the opening is stored across rows and derived within one (D4), an exact bill-run coincidence may be reported but a nearby instant may never be suggested (D7), and this change drops and renames nothing (D16).
- [x] 1.2 Re-confirm against production that the tables this change leaves in place are dead, immediately before writing the migration; verify `daily_cash_records`, `cash_withdrawals` and `public.expenses` each hold zero rows and `billing_live_from` is null at both outlets. **All four were true on 2026-08-26 by read-only query**, which is what decision 16 was written against; the re-run exists because months may pass between proposal and apply.
- [x] 1.4 Build an offline rehearsal that replays August 2026's real `manual_ledger_days` rows (19 per outlet, 2026-08-01 onward) and every settled bill from each outlet's first tablet day (Kalyani 2026-08-12, Kanchrapara 2026-08-14) through the derived reader; verify each month total lands on the figure already known from the manual ledger, and record any date that does not with its cause. Expect the opening-cash chain to break at Kalyani on 2026-08-12 and 2026-08-14 and across the 2026-08-18 to 08-20 gap; the rehearsal must **report** those breaks, and a rehearsal that silently repairs them has the wrong behaviour, not clean data.
- [x] 1.5 Settle open question 1 with the owner: whether an outlet's first observation takes an owner-supplied books-opening figure or starts from zero and absorbs the float into its first difference; verify the answer is written into `design.md` before the table is created.

## 2. Schema, policies and isolation

- [x] 2.1 Create `drawer_observations` with outlet, counted instant, recorded instant, stored opening, counted total, expected total, difference, approximate flag and tolerance window, recording account, correcting account, on-site evidence and off-site reason; verify a check constraint enforces `difference = counted_total - expected`, all money is `bigint` paise, and no client role holds insert, update or delete.
- [x] 2.2 Enforce the counted-instant bounds in the database: not after the recorded instant, strictly after the previous observation's counted instant at that outlet, and not before that outlet's earliest drawer activity; verify each is refused with a message naming what it collided with, and that the previous-observation check takes an advisory lock on `(outlet_id)` so two concurrent counts cannot interleave.
- [x] 2.3 Create `drawer_cash_out` with outlet, kind in `('collection','spend')`, a **signed non-zero** amount, occurrence instant, recording account, nullable observation link, nullable reason, and on-site evidence; verify a zero amount is refused, a negative `spend` is refused, a `spend` without a reason is refused, a negative `collection` is accepted with neither reason nor actor, and no client role writes it directly.
- [x] 2.4 Create `drawer_observation_adjustments` with the observation, the corrected counted total, a required reason, an instant and an account; verify an adjustment against an observation that no later observation has anchored on is refused, since that case is an edit rather than an adjustment.
- [x] 2.5 Create `ledger_day_verifications` with outlet, business date, account, instant and optional note, unique per outlet and date per account; verify re-verifying replaces nothing and is recorded as its own row.
- [x] 2.6 Add nullable `occurred_at` to `public.expenses` and `public.manual_ledger_expenses`; verify every existing row is untouched and reads back through `coalesce(occurred_at, created_at)`, and that `public.cash_withdrawals` is left untouched because nothing writes it and #12 drops it.
- [x] 2.7 Add RLS to all four new tables: Super Admin reaches every outlet, Franchise Admin reaches only the outlets their live assignment names, Biller and Employee reach none; verify assigned, cross-outlet, deactivated, ended-assignment and hand-crafted SELECT/INSERT/UPDATE/DELETE cases in `test:db` and `test:rls`, one case per table.
- [x] 2.8 Prove the Super Admin drawer reach is a grant and not an accident: verify a Super Admin holding no assignment writes an observation, a collection, a spend and an adjustment at both outlets, and that the identical request from a Franchise Admin at an unassigned outlet is refused. **This is the only path that will work in production on day one**: confirmed 2026-08-26 that the live assignments are one Biller and two Employees per outlet, with no Franchise Admin and no Super Admin assignment anywhere. A drawer reachable only through a Franchise Admin assignment would be reachable by nobody.
- [x] 2.9 Confirm the migration contains only `create table` and `add column ... null`; verify a grep of the migration finds no `drop`, no `rename`, no `alter ... type` and no `update`, because decision 16 is the revert story.
- [x] 2.10 Regenerate `src/data-access/database.types.ts`; verify `npm run db:types` leaves no unexpected generated-type diff.

## 3. The interval arithmetic

- [x] 3.1 Write the pure domain module computing expected total, difference and carry-forward in integer paise; verify a non-integer input throws rather than rounding, and that the module has no import from the data layer.
- [x] 3.2 Implement the receipts term as the latest accepted effective Cash allocation of settled bills whose payment instant falls in `(previous counted_at, this counted_at]`; verify a superseded original allocation and an earlier correction revision each contribute nothing, that UPI, Swiggy and Zomato contribute nothing, and that a payment exactly at the previous instant belongs to the earlier interval.
- [x] 3.3 Implement the expenses term over `coalesce(occurred_at, created_at)` restricted to cash; verify a spend at 18:10 and one at 23:00 fall on opposite sides of a 22:00 count.
- [x] 3.4 Implement the cash-out term excluding rows linked to the observation being written; verify an observation saved together with a ₹7,500 collection has that collection in neither its expected total nor its counted total, and that the next observation's opening is the counted total less ₹7,500.
- [x] 3.4a Prove the sign carries through the same term with no branch: verify a drawer counted at ₹450 with an amount of −1,000 leaves ₹1,450 and opens the next interval at ₹1,450, and verify a standalone negative movement inside an interval raises that interval's expected total by the same amount. Verify the domain function and the database agree on both.
- [x] 3.5 Compute expected inside the writing transaction from a `security definer` function that is the only insert path; verify a direct insert, a client-supplied expected total and a client-supplied difference are each refused.
- [x] 3.6 Prove the anchoring rule with a three-observation sequence where the middle one is ₹500 short; verify the third observation's opening and difference are identical to a run where the middle one balanced, so the shortfall does not propagate.
- [x] 3.7 Prove the stored-opening rule: verify that adjusting an earlier observation changes no later observation's stored opening, and that the surface reports the resulting break rather than repairing it.
- [x] 3.8 Prove the multi-day interval: verify an observation whose previous one was three days earlier sums three days of receipts, expenses and cash out by the same code path as a single evening.

## 4. Approximate times and what may be said about them

- [x] 4.1 Mark an observation approximate whenever its counted and recorded instants differ, with a plus-or-minus fifteen minute window and a control asserting certainty; verify the flag and window are stored, not recomputed on read.
- [x] 4.2 Compute the rupee tolerance from cash throughput inside the window; verify a ₹50 difference against ₹914 of nearby cash and a ₹3,100 difference against the same ₹914 render differently.
- [x] 4.3 Detect an exact coincidence between the difference and a contiguous run of adjacent cash bills, and report it naming those bills and their instants; verify the ₹854 case from `design.md` is reported and that a ₹500 difference against the same bill set is not.
- [x] 4.4 Assert the refusal directly: verify that for a difference with no exact match the surface emits no alternative instant, no ranked boundary and no balancing hint, in the rendered output and in whatever the component returns.
- [x] 4.5 Build the movable count boundary over nearby cash bills; verify dragging it updates the expected total and the difference live, and that the position is a stated instant rather than a chosen bill identity.
- [x] 4.6 Show both instants and the lag on every observation; verify the recorded instant is the server's and cannot be supplied by a client.

## 5. Corrections, exceptions and late arrivals

- [x] 5.1 Allow full edit of the most recent observation at an outlet with no reason and no trail on the row; verify the edit recomputes expected and difference and that an edit to any earlier observation is refused.
- [x] 5.2 Implement adjustments with a required reason; verify both the original and corrected figures, both accounts and the reason remain readable, and that the next observation's stored opening does not move.
- [x] 5.3 Record and surface reconciliation exceptions for cash whose payment or occurrence instant falls inside an already-observed interval; verify the exception names the rows, amounts, occurrence and arrival instants, and the difference that would have resulted.
- [x] 5.4 Handle the explaining case: verify a late arrival equal to a recorded excess leaves the recorded excess in place and marks it explained with its date.
- [x] 5.5 Implement acknowledge-with-a-note and record-a-fresh-observation as the two resolutions; verify neither alters a stored observation figure.
- [x] 5.6 Surface unsynced devices as an advisory on the count sheet, naming how many and since when, marking the expected figure possibly understated; verify a count is still accepted while a device is behind.
- [x] 5.7 Prove the whole-system rule: verify no code path recomputes a stored observation figure in response to a bill, an expense, a settlement or a sync.

## 6. The Cash drawer surface

- [x] 6.1 Build the surface opening on the running balance, with the last observation, what was left, and cash receipts and cash expenses since with their row counts; verify it opens on a balance and offers no date picker, and that it states how many days the pending interval covers once it exceeds one.
- [x] 6.2 Build the count sheet with its three inputs; verify the difference appears on entry of the counted amount before submission, states direction in words as well as by sign, and that a shortfall reads as negative.
- [x] 6.3 Build the time step with a `Just now` default and relative chips; verify choosing an earlier instant recomputes the expected figure live and states the cash excluded by that choice.
- [x] 6.4 Build the collection step inside the count sheet, showing what will be left as it is typed; verify no actor picker and no reason field are presented.
- [x] 6.5 Build standalone `Collect cash` with two fields; verify it records no reason and no actor, states the balance before and after, and says on its face that nothing is being verified.
- [x] 6.5a Make a negative amount announce itself on the keystroke that creates it, in both the count sheet's collection step and the standalone sheet; verify the warning appears before submission and not at it, that the stated action, the balance preview and the confirming control all flip to adding, and that a positive amount shows no warning. Verify by typing a minus into each surface, not only by unit-testing the helper.
- [x] 6.6 Build `Record a cash spend` as a secondary link with a required reason; verify it states that it will not enter the month's operating expenses, and that it is not reachable as prominently as a collection.
- [x] 6.7 Build the adjustment sheet for a locked observation; verify it names why the observation is locked, requires a reason, and states that the following observation re-anchors the balance so nothing after it moves.
- [x] 6.8 Build the exception card with its two resolutions; verify it names the arriving rows and what the difference would have been.
- [x] 6.9 Walk the surface on a phone and a tablet in light and dark themes; verify no layout reflow, that money never truncates, and that `npm run contrast` passes.

## 7. The derived ledger statement

- [x] 7.1 Build the derived day reading from bills, expenses, `aggregator_channel_days`, cash out and observations, with no stored row; verify a business date with nothing recorded still renders in full.
- [x] 7.2 Render the revenue section with Cash and UPI from allocations and each aggregator's gross, commission, net and settlement state; verify an undetermined commission reads as not known yet rather than nought, and that the month is presented as a ceiling while any day's commission is undetermined.
- [x] 7.3 Render the drawer section ordered by instant; verify an expense before an observation appears above its block and one after appears below it, and that a day with two observations shows two blocks.
- [x] 7.4 Render an unexplained variance as its own line inside the observation block; verify the section still adds to the closing balance with the variance present.
- [x] 7.5 Name the float left and the closing balance separately and never with one word; verify the worked example renders ₹1,450 left and ₹3,504 closing, and that the surface states the float is not the next day's opening. Verify the word "Kept" appears nowhere in the surface, its tests or the docs it updates.
- [x] 7.6 Mark a day with no observation as `carried` on both balances, naming when the drawer was last confirmed; verify the month view carries the same marker per row.
- [x] 7.7 Assert the surface has no editable figure; verify a test enumerates the rendered controls and finds only the date stepper, row expansion and verification.
- [x] 7.8 Measure the derived month against a real August at both outlets; verify the timing is recorded against open question 3, and that if it does not hold the remedy taken is a read model rather than a stored day row.

## 8. Verification

- [x] 8.1 Implement day verification recording account, instant and optional note; verify it freezes nothing and that a day computes and renders identically before and after.
- [x] 8.2 Detect and surface a change to a verified day's inputs; verify the day is marked changed since verified and names the figure that moved, including the case of an aggregator cycle settling afterwards.
- [x] 8.3 Verify each day joins a verification by its own action; verify no control verifies more than one day and that none exists for selecting a range.

## 9. Gates, navigation and the overlap

- [x] 9.1 Add `cash-drawer` and `ledger-statement` to `src/gates/registry.ts` as `live` for Super Admin and Franchise Admin shells; verify Biller and Employee shells mount neither and that no route resolves for them.
- [x] 9.2 Demote the manual ledger from the **primary** navigation entry — the one labelled `Ledger` — while leaving its gate `live`, its route resolving and **its own navigation entry in place**; verify the route renders in full and that both readings can be open at once. **Corrected 2026-08-27:** this task previously said to *remove* the entry, which contradicts decision 17 (*"two entries let the owner open both and compare a day"*) and the Risks section (*"Two ledger entries in the navigation during the overlap"*). It was followed literally once, and the result was a fallback reachable only by typing a URL — which is not a tab, and is a fallback nobody reaches at 22:00 when the new surface is behaving oddly. The manual ledger now sits after the derived statement as **Notebook**.
- [x] 9.3 Set the demo-gated Daily cash surface to `hidden` and delete its mock adapter usage from the navigation; verify the four-role demo walkthrough no longer offers a day close and still walks.
- [x] 9.4 Confirm no runtime toggle was introduced; verify a search of configuration, storage and the interface finds no control selecting between the two ledger readings, and that `docs/DEMO_MODE.md`'s build-time rule still holds.
- [x] 9.5 Update the demo fixtures so the mocked outlet carries observations, a collection, a spend, an approximate count with an exact coincidence, a genuine shortfall and a late-arrival exception; verify the demo walkthrough reaches every one of those states.

## 10. Docs, roadmap and gates

- [x] 10.1 Update `docs/SCREENS.md`, `docs/DATA_MODEL.md`, `docs/GLOSSARY.md`, `docs/ROLES_AND_PERMISSIONS.md`, `docs/OFFLINE_AND_SYNC.md`, `docs/LIMITATIONS.md`, `docs/DEMO_MODE.md`, `docs/OPERATIONS.md` and `docs/TESTING.md`; verify the glossary no longer defines a closing figure, that "Business date" is unchanged, that the `paid_at` skew limitation is recorded, and that no page still describes a day close as a thing that happens.
- [x] 10.2 Sweep every stale reference to `daily-cash-live` and `expenses-and-inventory-live` outside `openspec/changes/archive/`; verify each now points at this change or at #12, and that archived changes are left as the dated record they are.
- [x] 10.3 Amend the #11 and #12 rows, the dependency graph and the Wave E narrative in `openspec/changes/ROADMAP.md`; run `npm run roadmap:sync` and verify no hand-stamped status drift.
- [x] 10.4 Read `.github/workflows/` and run every job it runs, not a remembered list; verify the non-Docker gates and the Docker-backed gates each pass with recorded evidence. **`verify.yml` was read rather than remembered**, and every job it defines was run: `lint`, `format:check`, `typecheck`, `test`, `contrast`, `build`, `test:e2e`, then `supabase start` / `test:db` / `test:rls` / `test:e2e:auth` / `db:types` with the generated-type diff clean. **One step did not run and is reported as not run rather than as passing:** `functions:typecheck` needs Deno 2.x, which this machine does not have and which CI installs through `denoland/setup-deno@v2`. This change touches no Edge Function, so nothing in it is inside that gate's blast radius.
- [x] 10.5 Walk all four demo roles and both real shells on a phone and a tablet in both themes; verify no regression in billing, attendance or the aggregator surfaces.
- [x] 10.6 PHASE GATE — Cash is counted, not closed: a count taken at 22:00 mid-service is measured against cash received up to 22:00 and no further, and the cash rung afterwards opens the next interval; the same path records a count after two skipped days and states that it covers three; a count entered an hour later with an approximate time reports how much the timing could explain, names an exact bill-run coincidence as a fact, and proposes no instant when none matches, proved by a test asserting the absence; a collection takes an amount and an instant with no reason and no actor, a negative amount is cash added to a thin drawer and says so on the keystroke that makes it negative rather than at submission, and drawer cash spent on equipment takes a reason and leaves the month's operating expenses unchanged; a shortfall is recorded once and does not reach the next interval; an observation is editable until the next one anchors on it and only adjustable afterwards, with both figures readable; cash syncing into an observed interval reports beside the observation and never inside it, including the case where it explains a recorded excess; a Super Admin with no assignment counts at both outlets and the record says where they stood, while a Biller and an Employee are refused every drawer read and write by the database, proved by a hand-crafted request; the Ledger renders every day with no typed field, orders the drawer by instant, names the float left and the closing balance differently, and marks an uncounted day `carried`; the migration contains no drop and no rename, so the previous surface still works at its route; and the four-role demo walkthrough still walks.

## 11. The drawer's second pass, 2026-08-28

Everything in sections 1 to 10 is built and walked. This section is the owner's
reading of the surface afterwards, and it reverses two decisions taken above —
see `design.md` decisions 19 to 23 before starting, because 19 undoes half of 6.

- [x] 11.1 Lay the balance card out as decision 23 describes: the running total at the right end of the `In the drawer now` line, the qualifying chips directly beneath it, and the three-up strip below a divider; verify nothing else on the surface renders money left-aligned and that the shimmer still reserves the card's height without reflowing on arrival.
- [x] 11.2 Rename the three figures to `Last Left`, `Cash from Bills` and `Cash Expenses`, give the bills figure a leading `+` and render the expenses figure negative and in `--danger`; verify both signs survive a reader who cannot distinguish the tones, and that `npm run contrast` passes in light and dark.
- [x] 11.3 Rename the three actions to `Count & Collect`, `Only Collect` and `Other Spend`, and promote Other Spend from `ghost` to `secondary`; verify it still sits after Only Collect in the quieter row, so decision 5's separation survives while its invisibility does not.
- [x] 11.4 Add `listObservations(outletId, { before, limit })` to the drawer adapter, cursored on `counted_at` and implemented in both the mock and the Supabase adapter; verify a page boundary landing exactly on a shared instant neither duplicates nor skips a row, and that a count recorded between two page reads cannot appear twice.
- [x] 11.5 Make each recent count a disclosure on the `sync-event-row.tsx` pattern — header button, `aria-expanded`, rotating chevron, body unmounted while closed; verify the closed row carries the instant, the amount, the verdict including `matched`, and the opening break, and that the recorder, the away reason, the collection, the adjustments and the adjust control are **absent from the rendered output** rather than merely hidden.
- [x] 11.6 Move `Adjust` into the disclosure body as a `Button` with `size="phone"` and `variant="secondary"`; verify it matches what every other disclosure body in the app puts in the same position, and that it still appears only on an observation a later one has anchored on.
- [x] 11.7 Page the history in the surface: load the next page when a sentinel below the list comes into view, offer a control that does the same, and say when the oldest count has been reached; verify the control works with `IntersectionObserver` absent, that scrolling does not fire overlapping reads, and that switching outlet resets the list rather than appending to the previous outlet's.
- [x] 11.8 Remove the numbered legends from the count sheet and rename its three fields to `Collection time`, `Cash counted before collection` and `Cash collected, if any`; verify the collection field starts at `0` so the leaving preview is correct before anything is typed, and that a `0` collection writes no cash-out row.
- [x] 11.9 Replace the time options with `Now`, `15 min ago`, `30 min ago` and an explicit date-and-time control; verify a stated instant drives the same interval arithmetic as a relative one, that an instant in the future or at or before the previous count is refused by the sheet with a sentence naming what it collided with, and that neither refusal reaches the database as a constraint message.
- [x] 11.10 Make every count approximate (decision 19): always send `certain: false`, delete the `I'm sure` control and delete the per-observation `~ approx` marker; verify the tolerance window is stated once beside the chosen instant, that `timingCouldExplainPaise` is computed for a count taken now, and that the database still marks the row approximate because the counted instant is strictly earlier than the recorded one. **`p_certain` stays in the migration** — verify no migration file is edited. **No migration was edited**: `p_certain` keeps its parameter and its default, and the surface simply never sends `true` (design D19). The database still marks every row approximate because the counted instant is captured when the sheet opens and is therefore strictly earlier than the server instant that records it.
- [x] 11.11 Read one position when each of the count, collect and spend sheets opens, through `readPosition()` and no other path; verify the read blocks no field and no save control, that nothing watches or samples in the background, and that `src/lib/geolocation.ts` is still the only module in the app touching `navigator.geolocation`.
- [x] 11.12 Show the reason field exactly when the recorder is not shown to be inside the fence, and require it there; verify an inside-the-fence sheet renders no reason field at all, that outside it the distance is named, that no-fix cases (denied, unavailable, timeout, unsupported, and an outlet with no captured position) each say why they could not tell, and that a blank reason is refused **by the sheet** with no write attempted.
- [x] 11.13 Delete the hardcoded `'recorded from the app'` away reason from the collect and spend paths; verify a grep of `src/` finds no constant standing in for a recorder's reason, and that an on-site movement now writes `away_reason` null with `recorded_on_site` true.
- [x] 11.14 Prove the defect this pass fixes actually existed: verify against a local database that an observation written with no coordinates and an empty reason is refused by `drawer_observations_away_needs_a_reason`, and that the same call carrying an inside-the-fence position is accepted with no reason at all. **Already proved, and it passes**: `supabase/tests/41_cash_drawer.sql` asserts all three cases — *"a count recorded outside the fence asks for a reason FIRST"*, *"a count with NO position asks for one too"*, and *"the recorder was inside the fence, so no reason was asked for"*. Re-run on a fresh stack 2026-08-28: `npm run test:db` reports **All tests successful**. The database was always right; only the surface was wrong, which is exactly why the defect was invisible until somebody read the sheet.
- [x] 11.15 Update `docs/SCREENS.md` (the surface's layout, the three action names, every-count-is-approximate, the detected position, the paged disclosure list), `docs/SECURITY_AND_PRIVACY.md` and the header of `src/lib/geolocation.ts` (the drawer is a fourth caller, and both places state their list as exhaustive); verify no page still describes an `I'm sure` control or an optional away box.
- [x] 11.16 Re-run every job `.github/workflows/verify.yml` defines, read from the file rather than remembered, and walk the drawer on a phone viewport in both themes; verify the four-role demo walkthrough still walks and that billing, attendance and the aggregator surfaces are untouched. **`verify.yml` was read rather than remembered**, and every job it defines was run on 2026-08-28: `lint` (0 errors), `format:check`, `typecheck`, `functions:typecheck`, `test` (128 files, 1523 tests), `contrast` (52 pairs, both themes), `build`, `test:e2e` (248 passed), then `supabase db reset` / `test:db` (all successful) / `test:rls` (244 passed across the five phases) / `test:e2e:auth` (21 passed) / `db:types` with a clean diff. **`functions:typecheck` ran this time**: Deno 2.9.5 is installed at `~/.deno/bin` but is not on the shell PATH, which is why the previous pass recorded it as not run. The drawer was walked on a 375x812 viewport in both themes: the balance card, the three renamed actions, a disclosure opened and closed, the count sheet inside the fence, outside it, and with permission denied.

## 12. Expenses are read where they are written, 2026-08-28

Found in production by the owner hours after this change deployed, and one
tenancy defect found beside it. Read `design.md` decisions 24 to 26 first: 24
reverses a premise rather than a decision, and 26 is a live breach of the first
hard rule in `AGENTS.md`.

- [x] 12.1 Establish the premise against production before writing anything; verify by read-only query that `public.expenses` holds zero rows while `manual_ledger_expenses` holds the live ones. **Measured 2026-08-28: 0 rows against 118, 65 of them cash, spanning 2026-08-01 to 2026-08-28.** The proposal recorded the same 0 and read it as nothing to migrate rather than as nothing writes this.
- [x] 12.2 Create `public.effective_expenses`, unioning `public.expenses` with the un-voided rows of `manual_ledger_expenses` and normalising `payment_method = 'cash'` and `is_cash` to one boolean; verify it carries `security_invoker = true`, because without it the view runs as its owner, RLS on the base tables is bypassed, and any session reads every outlet's expenses through it.
- [x] 12.3 Point `drawer_cash_expenses_paise()` and the derived ledger's day read at the view; verify the interval convention, the `coalesce(occurred_at, created_at)` fallback and the cash-only rule are unchanged, and that a UPI expense still moves no drawer figure.
- [x] 12.4 Add `app_may_reach_drawer()` to all three interval readers (decision 26); verify a Biller and an Employee read nought from each at every outlet including their own, that the Super Admin holding no assignment still reads every term at both, and that `record_drawer_observation()` is unaffected because it checks the same predicate first.
- [x] 12.5 Mirror the view in the mock (`src/data-access/mock/effective-expenses.ts`) and route both mock readers through it; verify the demo shows an expense recorded through the Expenses tab on the Ledger and in the drawer, which it did not before — the mock reading one array while the database reads a union is how this defect survived its first review.
- [x] 12.6 Prove both defects on a database **before** the fix rather than reasoning about them: verify `drawer_cash_expenses_paise` returns 0 for a real ₹900 cash expense written the way the app writes one, and that a Biller whose `app_may_reach_drawer` is false reads a non-zero receipts figure. **Both measured 2026-08-28 on a fresh local stack: 0 paise, and ₹1,005 respectively.**
- [x] 12.7 Pin the fix at three levels: pgTAP for the view, the arithmetic and the refusals (`42_expenses_are_read_where_they_are_written.sql`); a REST probe driving the **real** ledger adapter over HTTP, because a hand-rolled select would pass while the app stayed broken; and a mock regression that records through `manualLedger.createExpense` rather than against the store. Verify the mock regression fails against the mocks as they shipped — **3 of its 4 cases do**, the fourth being the UPI guard that must pass either way.
- [x] 12.8 Correct the `manual-ledger` spec delta scenario that forbade this outright; verify it now refuses a drawer *belief* taken from a notebook day row, which is decision 18 unchanged, and requires the notebook's *expenses* to be counted until #12 carries them across.
- [x] 12.9 Move Expenses ahead of the Ledger in the owner and manager shells, so the nightly walk reads Billing, Drawer, Expenses, Ledger, Notebook; verify the demo-gated Expenses entry still wins the label dedupe in demo mode and the live one in the real app, and that the Biller and Employee shells are untouched.
- [x] 12.10 Re-run every job `.github/workflows/verify.yml` defines, on a database reset from scratch; verify the drawer-writes phase resolves `@/` now that it drives the real adapter, and that `db:types` is clean but for the new view.
